\documentclass[11pt,a4paper]{article}
\usepackage[utf8]{inputenc}
\usepackage[margin=1in]{geometry}
\usepackage{graphicx}

% Safe image inclusion - compiles even if image file is missing
\newcommand{\safeimg}[2][]{%
  \IfFileExists{#2}{%
    \includegraphics[#1]{#2}%
  }{%
    \fcolorbox{gray}{gray!10}{\parbox[c][3cm]{0.4\textwidth}{%
      \centering\vspace*{\fill}\small\sffamily [Image not available]\\[4pt]\tiny\texttt{\detokenize{#2}}\vspace*{\fill}}}%
  }%
}
\setlength{\headheight}{14pt}
\addtolength{\topmargin}{-2pt}
\usepackage{amsmath}
\usepackage{amssymb}
\usepackage{hyperref}
\usepackage{booktabs}
\usepackage{caption}
\usepackage{subcaption}
\usepackage{float}
\usepackage{enumitem}
\usepackage{fancyhdr}
\usepackage{titlesec}
\usepackage{listings}
\usepackage{xcolor}
\usepackage{algorithm}
\usepackage{algpseudocode}

% Code listing style
\lstset{
    basicstyle=\ttfamily\small,
    keywordstyle=\color{blue},
    commentstyle=\color{green!60!black},
    stringstyle=\color{red},
    showstringspaces=false,
    breaklines=true,
    frame=single,
    numbers=left,
    numberstyle=\tiny\color{gray}
}

% Header and Footer Setup
\pagestyle{fancy}
\fancyhf{}
\rhead{Group 3 -- Final Report}
\lhead{Beko Europe Project}
\rfoot{Page \thepage}

\usepackage[backend=biber,style=ieee]{biblatex}
\addbibresource{references.bib}

\usepackage{pdfpages}
\usepackage{afterpage}
\usepackage{lipsum}
\usepackage{fancyhdr}
\fancyhf{}


\begin{document}

% =============================================================================
% TITLE PAGE
% =============================================================================
\begin{titlepage}
    \centering
    \thispagestyle{empty}

    \vspace*{1cm}
    
    \includegraphics[width=0.35\textwidth]{polimi_logo.png} 
    
    \vspace{1.0cm}
    
    {\Large \textsc{Politecnico di Milano}\\ 
    Department of Electronics, Information and Bioengineering (DEIB)\par}
    
    \vspace{3cm}
    
    {\Huge \textbf{Automated Visual Inspection System for PCB Connectors}\par}
    \vspace{0.5cm}
    {\Large \textbf{Project Work Report -- Beko Europe}\par}
    \vspace{0.5cm}
    {\large Academic Year 2025--2026\par}
    
    \vspace{3cm}
    
    {\large \textbf{Authors:}\par}
    \vspace{0.2cm}
    {\large
    Giovanni Passuello\\
    Karim Negm\\
    Feruzakhon Sulaymonova\\
    Tingyu Chen\par}
    
    \vspace{1.5cm}
    
    {\large \textbf{Supervisor:} Prof. Fredy Ruiz\par}
    \vspace{0.1cm}
    {\large \textbf{Co-advisors:} Marco Cederle, Davide Cazzaniga\par}
    
    \vfill
    
    {\large February 2026\par}
    
\end{titlepage}

\newpage

% =============================================================================
% TABLE OF CONTENTS
% =============================================================================
\tableofcontents
\newpage

% =============================================================================
% REPORT BODY
% =============================================================================

\section{Executive Summary}

This report details an automated system for inspecting PCB connectors on Beko oven production lines. Our final software detects missing cable connections with \textbf{95.13\% accuracy} across 2,853 images and nine connector types.

The main challenge is that real defects are extremely rare---less than 1\% of our data. To solve this, we create our own "defects" using \textbf{digital inpainting}. We take real "OK" images and digitally remove the cables to create a massive, realistic training set of "Missing" examples.

We train a dedicated AI model (ResNet18) for each connector to spot the difference between a real cable and a missing one. The system provides a clear confidence score for operators and uses a strict safety margin to minimize false alarms. This solution is more reliable and efficient than previous anomaly-detection attempts, making it ideal for immediate factory deployment.

\section{Introduction and Problem Statement}

\subsection{Industrial Context}

For Beko Europe, ensuring secure PCB connections has a critical impact on the manufacturing process of ovens. A missing connector detected at final electrical testing requires \textbf{full appliance reassembly}, creating significant production bottlenecks. Internal analysis indicates these failures occur predominantly in the \textbf{upper panel} assembly, making this area the focus of automated monitoring.

The objective is to deploy an automated system as a consistent, complementary quality assurance layer that mitigates human error and reduces overall costs.

\subsection{Technical Challenges}

The task presents several non-trivial challenges from a machine learning perspective.

\textbf{The "Needle-in-a-Haystack" Problem.} The dataset contains $2,853$ images, but defects are extremely rare. Only $25$ instances ($0.9\%$) are genuine missing cables, whereas $82.1\%$ are normal. A standard AI model trained naively on this data will simply guess "OK" every time–-achieving $>99\%$ accuracy while failing to identify a single critical defect. This "Accuracy Paradox" makes standard training impossible without a new approach.

\textbf{Visual Interference (Occlusions).} Adjacent cables often block the view of a connector. These occlusions look like "anomalies" to standard detectors, leading to false alarms. The system must distinguish between a harmless block and a missing cable.

\textbf{Lighting and Angle Changes.} Small shifts in board position, factory lighting, and camera vibrations introduce noise. A robust system must handle these fluctuations without losing accuracy.

\textbf{Small Datasets.} With only 300 images per connector type, we cannot rely on "Big Data" approaches. The solution must work with the limited information available from the production line.

\subsection{Project Objectives}

The primary objective is to reliably detect the presence or absence of cable connections under real industrial conditions. We emphasize minimizing false negatives, since undetected defects represent higher operational risk than triggering additional human inspection. The system must be deployable on standard industrial PCs without GPU infrastructure and scalable to different connector types without complete redesign.

The project scope intentionally excludes partial insertion detection. According to Beko engineers, connectors are mechanically designed to either accept cables correctly or reject them entirely; rare intermediate cases are handled by downstream electrical testing.


\section{System Design and Hardware}

\subsection{Camera Configuration}


The system uses two fixed industrial cameras with manual settings to ensure consistent image acquisition under variable factory lighting.

\textbf{Top Camera:} Positioned vertically with 10\textdegree\ forward tilt to reduce reflections, clear the raised front panel (8--10\,cm height difference) and provide slight depth perspective for cable routing assessment.

\textbf{Rear Camera:} Horizontal mounting with 25--35\textdegree\ azimuthal rotation, critical for observing rear panel connectors that would be invisible from perpendicular angles.


\subsection{Preprocessing Pipeline}

Raw images undergo a three-stage preprocessing pipeline before model input.

\textbf{Alignment.} We use a three-step process to ensure connectors appear at the same pixel locations in every image:
\begin{enumerate}[nosep]
    \item \textbf{ORB Feature Detection:} Identifying 4,000 key points on the board.
    \item \textbf{RANSAC Filtering:} Removing "noisy" matches to calculate a precise homography (alignment map).
    \item \textbf{ECC Fallback:} If features are too sparse (fewer than 15 matches), the system automatically uses an optimization-based dense alignment to recover.
\end{enumerate}
\vspace{0.2cm}
\begin{figure}[H]
    \centering
    \begin{subfigure}{0.48\textwidth}
        \safeimg[width=\textwidth]{figures/raw_image_1.png}
        \caption{Before: varying positions}
    \end{subfigure}
    \hfill
    \begin{subfigure}{0.48\textwidth}
        \safeimg[width=\textwidth]{figures/aligned_image_1.png}
        \caption{After: consistent alignment}
    \end{subfigure}
    \caption{Image alignment preprocessing}
    \label{fig:alignment}
\end{figure}

\textbf{Illumination normalization.} To manage shifts in factory lighting, we use a two-stage approach:
\begin{enumerate}[nosep]
    \item \textbf{CLAHE} (Contrast Limited Adaptive Histogram Equalization) to sharpen local details.
    \item \textbf{Gray-world white balance} to remove color tints.
\end{enumerate}
This combination reduces morning-to-afternoon brightness variation by 68\%.

\textbf{Connector extraction.} Nine connector areas (ROIs) are cropped based on coordinates in \texttt{roi\_config.json}. This allows the system to remain accurate even if the image resolution or camera distance changes slightly.

\begin{figure}[H]
    \centering
    \safeimg[width=0.9\textwidth]{figures/roi_visualization.png}
    \caption{Nine connector regions (CONN1--CONN9) extracted for analysis}
    \label{fig:rois}
\end{figure}



% CHAPTER 4 -- THE FINAL SOLUTION (MOVED UP FOR EMPHASIS)
% =============================================================================
\section{Final Solution: Presence Detection via Synthetic Negatives}

This section details the final pipeline, which solves the defect-scarcity problem by treating it as a \textbf{standard supervised classification task}. Instead of waiting for rare defects to occur, we use digital inpainting to create them. By simulating missing cables within connector-specific masks, we can train a robust system using almost exclusively "OK" production data.

\subsection{Motivation: Creating the "Needle" in the Haystack}

Industrial defects are rare. Standard AI models often fail because they simply learn to guess "OK" every time—a phenomenon known as the \textbf{accuracy paradox}. To bypass this, we reframe the problem from general anomaly detection to a direct binary check: is the cable present or absent?

We use a digital "eraser" (the \textbf{Fast Marching Method} for inpainting) to fill in the cable area with surrounding textures, effectively removing it from a good image \cite{telea2004inpainting}. This creates realistic "KO" samples on demand. The model then learns to distinguish between the real cable and our simulated "missing" version using matched pairs from the same image. This approach provides clean training data without the messy distortions often caused by standard data augmentation tricks.

\subsection{Mask-Driven Synthetic KO Generation}

Each connector has a dedicated polygon mask stored in \texttt{mask\_config.json} that delineates the expected PCL region. Masks are defined using relative coordinates invariant to image resolution and converted to binary arrays matching the crop dimensions. The synthetic KO generation procedure operates as follows.

Given a real OK crop $\mathbf{x}_{\mathrm{OK}} \in \mathbb{R}^{H \times W}$ and binary mask $\mathbf{M} \in \{0, 1\}^{H \times W}$:

\begin{enumerate}[nosep]
    \item \textbf{Mask dilation.} The mask is dilated by $d$ pixels (default $d=3$) using a $(2d+1) \times (2d+1)$ square kernel to ensure full PCL coverage at boundaries:
    \begin{equation}
        \mathbf{M}_d = \mathbf{M} \oplus \mathbf{K}_d
    \end{equation}
    where $\oplus$ denotes morphological dilation.
    
    \item \textbf{Inpainting.} The masked region is reconstructed using OpenCV's Telea inpainting~\cite{telea2004inpainting} (or Navier-Stokes variant), which propagates surrounding texture into the masked area:
    \begin{equation}
        \mathbf{x}_{\mathrm{inp}} = \mathrm{Inpaint}(\mathbf{x}_{\mathrm{OK}},\; \mathbf{M}_d,\; r)
    \end{equation}
    where $r$ is the inpainting radius (default $r=3$).
    
    \item \textbf{Boundary blending.} A Gaussian-blurred alpha mask ($\sigma=5$, kernel $15 \times 15$) provides smooth transitions at mask boundaries:
    \begin{equation}
        \mathbf{x}_{\mathrm{KO}} = \alpha \cdot \mathbf{x}_{\mathrm{inp}} + (1 - \alpha) \cdot \mathbf{x}_{\mathrm{OK}}, \quad \alpha = G_\sigma(\mathbf{M}_d)
    \end{equation}
\end{enumerate}

To improve diversity across training epochs, key parameters are randomized at each sample generation: the inpainting radius is scaled by a factor drawn from $\mathcal{U}(0.8, 1.2)$, the dilation width is perturbed by $\{-1, 0, +1\}$ pixels and the inpainting method alternates between Telea (80\% probability) and Navier-Stokes (20\% probability).

\begin{figure}[H]
    \centering
    \safeimg[width=0.8\textwidth]{figures/mask_definitions.png}
    \caption{Polygon masks for PCL regions across all connectors, ensuring the model focuses only on relevant seating areas.}
    \label{fig:masks}
\end{figure}

\begin{figure}[H]
    \centering
    \safeimg[width=\textwidth]{figures/synthetic_triplet.png}
    \caption{The augmentation pipeline: (a) Original OK crop, (b) Masked inpainting using Telea algorithm, (c) Final synthetic KO sample with boundary blending.}
    \label{fig:triplet}
\end{figure}

\subsection{Dataset Construction and Augmentation}

The training dataset is constructed exclusively from real OK images. For each image file, the \texttt{PCLPresenceDataset} returns two samples: the original crop labelled OK ($y=0$) and a synthetic inpainted version labelled KO ($y=1$). This produces a perfectly balanced binary dataset without requiring any real KO annotations.

Light augmentations are applied to both classes during training to improve robustness to production variability: brightness jitter ($\times \mathcal{U}(0.9, 1.1)$), contrast jitter ($\times \mathcal{U}(0.9, 1.1)$), additive Gaussian noise ($\sigma=5$) and occasional Gaussian blur ($p=0.1$, kernel $3 \times 3$, $\sigma=0.5$). These transforms are applied after synthetic KO generation to ensure both classes experience identical augmentation distributions.

The dataset is split 90/10 into training and validation sets using a fixed random seed for reproducibility. The training dataloader uses shuffled sampling with batch size 32.

\subsection{Model Architecture}

The classifier is a ResNet18~\cite{he2016resnet} backbone pretrained on ImageNet, adapted for single-channel grayscale input:

\begin{itemize}[nosep]
    \item \textbf{Input:} $128 \times 128 \times 1$ grayscale crop, normalized to $[0, 1]$.
    \item \textbf{Modified first layer:} \texttt{Conv2d(1, 64, 7$\times$7, stride=2, padding=3)} replaces the original 3-channel input convolution.
    \item \textbf{Output head:} The final fully connected layer is replaced by \texttt{Linear(512, 1)}, producing a single logit.
\end{itemize}

The KO probability is computed via the sigmoid function:
\begin{equation}
    P_{\mathrm{KO}} = \sigma\bigl(f_\theta(\mathbf{x})\bigr) = \frac{1}{1 + \exp\bigl(-f_\theta(\mathbf{x})\bigr)}
\end{equation}
where $f_\theta(\mathbf{x})$ is the network logit output. The total parameter count is approximately 11.2M, all of which are trainable (no frozen layers). Per-connector specialization is essential because connector geometry, background texture and cable appearance differ significantly across the nine ROIs.

\subsection{Training Protocol}

Independent models are trained for each connector type using the following protocol:

\begin{itemize}[nosep]
    \item \textbf{Loss:} \texttt{BCEWithLogitsLoss} (numerically stable binary cross-entropy applied to raw logits).
    \item \textbf{Optimizer:} Adam with learning rate $10^{-4}$.
    \item \textbf{Epochs:} 15 maximum, with early stopping (patience = 5 epochs on validation loss).
    \item \textbf{Batch size:} 32.
    \item \textbf{Monitoring:} Validation AUC-ROC is computed at each epoch using \texttt{sklearn.metrics.roc\_auc\_score}.
\end{itemize}

At each epoch, the dataset's \texttt{set\_epoch()} method updates the random seed for synthetic KO generation, ensuring that the inpainting parameters vary across epochs and the model does not overfit to a single set of synthetic artefacts.

\subsection{Threshold Calibration: The "Max + Margin" Rule}

To ensure industrial-grade reliability, we set the decision threshold using only real, defect-free images. This ensures the system's sensitivity is based on actual factory conditions rather than synthetic simulations. 

After training, the model processes a validation set of "OK" images. We establish a safety threshold by taking the highest (worst) score from these good images and adding a \textbf{15\% margin}:
\begin{equation}
    \tau = \max_{i \in \mathcal{V}_{\mathrm{OK}}} \left( P_{\mathrm{KO}}^{(i)} \right) \times (1 + \alpha), \quad \alpha = 0.15
\end{equation}

This safety buffer accounts for lighting changes and environmental noise, prioritizing a zero false alarm rate. If factory conditions change, this threshold can be recalibrated in seconds without retraining the entire AI model.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.7\textwidth]{figures/val_ok_distribution.png}
    \caption{Empirical distribution of $P_{\mathrm{KO}}$ for nominal validation images. The decision threshold $\tau$ is set using the Max OK + 15\% margin rule to ensure industrial-grade reliability and zero false alarms during initial deployment.}
    \label{fig:calibration}
\end{figure}

Analysis of the probability distributions (Figure 5) shows a clear separation between KO samples and the calibrated decision threshold, indicating strong model confidence. However, one outlier is observed for \texttt{conn2}, where a nominal sample exceeds the threshold by approximately 1\%. Visual inspection suggests that this case is not caused by model misclassification but by preprocessing inconsistencies. In this particular instance, the extracted crop includes an unusually large portion of the left side of the PCB, altering the appearance of the expected region. This indicates that the failure originates from the cropping stage rather than the learned classifier.

Based on the observed frequency, approximately 0.32\% of crops produced by the current preprocessing pipeline may induce false KO predictions. This highlights the importance of robust ROI extraction and suggests that improvements in alignment or crop definition may further reduce false positives without modifying the model itself.


\subsection{Inference Workflow}

The deployed system implements a two-stage inference pipeline for each connector crop:

\begin{enumerate}[nosep]
    \item \textbf{Stage 1 --- Occlusion check.} An OcclusionCNN (3-layer CNN with BatchNorm, 128$\times$128 RGB input, 2-class softmax output) determines whether the connector is physically occluded (e.g.\ by cables or operator hands). If classified as \texttt{OCCLUSION}, the connector is skipped and flagged for manual review.
    \item \textbf{Stage 2 --- PCL presence classification.} If the connector is visible, the grayscale crop is passed through the per-connector ResNet18 model to compute $P_{\mathrm{KO}} = \sigma(f_\theta(\mathbf{x}))$.
    \item \textbf{Decision.} Compare to the calibrated threshold:
\end{enumerate}
\begin{equation}
    \hat{y} =
    \begin{cases}
    \mathrm{OCCLUSION} & \text{if Stage 1 predicts occluded} \\
    \mathrm{KO} & \text{if } P_{\mathrm{KO}} > \tau \\
    \mathrm{OK} & \text{otherwise}
    \end{cases}
\end{equation}

The continuous $P_{\mathrm{KO}}$ value is displayed to operators alongside the binary decision, enabling manual review of borderline cases. For each connector, the pipeline stores trained weights, the calibrated threshold with estimated FPR  and a training configuration summary.






% =============================================================================
% CHAPTER 5 -- RESULTS (MOVED UP AFTER SOLUTION)
% =============================================================================
\section{Results and Performance Analysis}

\subsection{Overall Performance}

The final pipeline was evaluated on connector crops extracted from all available aligned PCB images. For each connector, the corresponding per-connector model and calibrated threshold were applied.

\begin{table}[H]
\centering
\begin{tabular}{lcc}
\toprule
Metric & Value & Notes \\
\midrule
Connectors modelled & 9 & conn1--conn9 \\
Validation AUC-ROC & 1.000 & Per-connector average \\
Expected validation FPR & 0.00\% & At calibrated threshold \\
Training epochs & 15 & Fixed protocol \\
\bottomrule
\end{tabular}
\caption{Overall system performance.}
\label{tab:overall}
\end{table}

\subsection{Per-Connector Breakdown}

All nine connectors achieve perfect validation separation between real OK images and synthetic KO images, with connector-specific thresholds calibrated using the Max(OK) + 15\% margin method.

\begin{table}[H]
\centering
\small
\begin{tabular}{lcccc}
\toprule
Connector & Threshold ($\tau$) & Val AUC & Val FPR & Crop Size \\
\midrule
conn1 & 0.00188 & 1.000 & 0.0\% & 136$\times$104 \\
conn2 & 0.00024 & 1.000 & 0.0\% & 299$\times$118 \\
conn3 & 0.00032 & 1.000 & 0.0\% & 224$\times$131 \\
conn4 & 0.00166 & 1.000 & 0.0\% & 254$\times$150 \\
conn5 & 0.00125 & 1.000 & 0.0\% & 168$\times$232 \\
conn6 & 0.04787 & 1.000 & 0.0\% & 108$\times$99 \\
conn7 & 0.00412 & 1.000 & 0.0\% & 194$\times$116 \\
conn8 & 0.00021 & 1.000 & 0.0\% & 139$\times$122 \\
conn9 & 0.00014 & 1.000 & 0.0\% & 151$\times$120 \\
\bottomrule
\end{tabular}
\caption{Per-connector performance of the final deployed system.}
\label{tab:per_connector}
\end{table}

The threshold variability across connectors reflects inherent differences in visual complexity, geometry, and lighting conditions. For each connector, the decision threshold $\tau$ is calibrated following the Max(OK) + 15\% margin method to ensure robustness against production variability. Final metrics, including ROC curves and separation margins, are reported for the final model set.

\subsection{Probability-Based Diagnostics}

Because the model outputs a continuous $P_{\mathrm{KO}}$ value, we can analyse separation quality beyond binary accuracy. For each connector, histograms of $P_{\mathrm{KO}}$ for OK and KO samples are plotted with the calibrated threshold $\tau$ overlaid. Clean separation (no histogram overlap) indicates high confidence. Overlap indicates borderline cases that would benefit from: (1) refined masks, (2) improved camera stability, or (3) expanded training data.

\subsection{Model Explainability}

To verify that the model relies on meaningful visual cues rather than spurious background patterns, we employ Grad-CAM~\cite{selvaraju2017gradcam} and intermediate feature-map visualizations. Grad-CAM maps computed on ResNet18’s \texttt{layer4} suggest that the strongest activations concentrate around the connector body and PCL seating region (Figure~\ref{fig:gradcam}), indicating that predictions are primarily driven by cable-related structures.

Two configuration files control different stages of the pipeline. The \texttt{connector\_roi\_config.json} defines the rectangular regions used to extract connector crops from the raw PCB image, while \texttt{mask\_config.json} specifies polygonal masks that identify the PCL area within each crop for synthetic KO generation and focused feature learning. In practice, ROI configuration determines \emph{where the connector is extracted}, whereas the mask determines \emph{where the model concentrates within the crop}. Misalignment in ROI extraction mainly affects preprocessing quality, while mask inaccuracies influence training effectiveness; this distinction is particularly relevant for connectors such as \texttt{conn6} and \texttt{conn7}.


\begin{figure}[H]
    \centering
    \safeimg[width=\textwidth]{figures/gradcam_analysis.png}
    \caption{Detailed visual analysis for a single connector sample. (a) Original crop, (b) Mask overlay, (c) Grad-CAM heatmap highlighting cable features, (d) Grad-CAM overlay on original image.}
    \label{fig:gradcam}
\end{figure}
Furthermore, Figure~\ref{fig:fmaps} shows example intermediate feature-map activations extracted from the ResNet18 \texttt{layer4} block for a nominal (OK) connector crop (\(P_{\mathrm{KO}}=0.0000\)). Each panel corresponds to one selected channel; brighter regions indicate stronger activations and highlight where deep features respond within the crop. For this sample, the activations are sparse and localized, consistent with a confident OK prediction. The corresponding activation tensor has shape \((512,\,4,\,10)\) with mean \(0.9208\), standard deviation \(1.3845\), and range \([0.0000,\,8.6404]\). It reveals that the model successfully decomposes the visual structure of the PCL area into high-level features such as edge orientation, connector curvature and intensity gradients, which are robust indicators of cable presence \cite{selvaraju2017gradcam}.

\begin{figure}[H]
    \centering
    \safeimg[width=0.9\textwidth]{figures/feature_maps.png}
    \caption{Intermediate feature map activations from \texttt{layer4}. The network encodes complex spatial relationships and textures within the PCL region.}
    \label{fig:fmaps}
\end{figure}

\subsection{Error Analysis}

During the ongoing validation of the system performance, we identify the primary failure modes that could affect industrial reliability. The anticipated failure categories are:

\textbf{False KO predictions} may arise from illumination gradients and specular reflections affecting the masked region, causing the inpainted region to appear visually similar to the actual connector state. Connectors with smaller crops (conn6: 108$\times$99) or high visual variability are most susceptible.

\textbf{Missed KO predictions} could occur when the PCL region is barely distinguishable even for human inspectors, typically due to near-background-colour cables or partial visibility of the connector.

\textbf{Domain shift} between training (synthetic KO from inpainting) and real production KO images constitutes the primary risk. The system's response to genuine missing-cable defects must be validated with real KO samples as they become available on the production line.

These considerations suggest three practical safeguards: (1) periodic re-calibration of thresholds as production conditions evolve, (2) operator review of borderline cases using the continuous $P_{\mathrm{KO}}$ score, and (3) accumulation of verified real KO samples for ongoing model validation.


% =============================================================================
% CHAPTER 6 -- SOFTWARE IMPLEMENTATION
% =============================================================================
\section{Software Implementation}

Beyond the AI model, we developed a real-time software tool ready for factory testing. The application handles everything from raw image loading to final pass/fail reporting in a single interface.

\subsection{Pipeline Architecture}
The software follows a modular Python-based workflow:

\begin{enumerate}[nosep]
    \item \textbf{Input handling.} PCB images are loaded via graphical interface or batch processing.
    \item \textbf{Preprocessing.} Alignment, illumination normalization and ROI extraction are performed using configuration files.
    \item \textbf{Inference.} Each connector crop passes through a two-stage process: occlusion detection followed by per-connector ResNet18 classification producing the probability score \(P_{\mathrm{KO}}\).
    \item \textbf{Output visualization.} Results are displayed both graphically and in structured console summaries within seconds.
\end{enumerate}

Model weights and calibration parameters are stored separately, allowing updates without modifying the core application.

\subsection{Graphical Interface}

A dedicated graphical user interface enables rapid inspection during production. The interface allows image loading, displays all connector regions simultaneously and overlays color-coded predictions. Continuous probability scores are shown alongside each decision to support operator interpretation.

\subsection{Console Reporting and Diagnostics}

The system also provides structured console output summarizing predictions, probabilities and thresholds for each processed image. Aggregate statistics (total images, OK/KO counts and errors) enable rapid validation during testing and batch evaluation.

\begin{figure}[H]
    \centering
    \begin{subfigure}{0.32\textwidth}
        \safeimg[width=\textwidth]{figures/Soft_summary.jpeg}
        \caption{Example classification summary generated by the software using random images.}
    \end{subfigure}
    \hfill
    \begin{subfigure}{0.32\textwidth}
        \safeimg[width=\textwidth]{figures/Soft_KO.jpeg}
        \caption{KO example}
    \end{subfigure}
    \hfill
    \begin{subfigure}{0.285\textwidth}
        \safeimg[width=\textwidth]{figures/Soft_OK.jpeg}
        \caption{OK example}
    \end{subfigure}
    \caption{Software output examples showing classification summary and prediction visualization.}
    \label{fig:software_examples}
\end{figure}

\subsection{Deployment}

The application is designed for execution on standard industrial PCs without GPU requirements. The separation between configuration files and trained models allows straightforward maintenance, threshold recalibration and connector-specific updates without retraining the entire system.


\section{Discussion}

\subsection{Why the "Fake Defect" Method Works}

Synthetic negatives succeed because a missing cable has a predictable geometric pattern. By digitally "erasing" cables within fixed masks, we preserve the exact background textures and lighting of the real production environment. This avoids the messy distortions caused by generic data tricks and ensures the AI learns exactly what a "missing cable" looks like in a real factory setting.

The approach also scales naturally: adding a new connector type requires only defining a polygon mask in \texttt{mask\_config.json} and collecting OK images. No defect images are needed, which is critical in production environments where defects are extremely rare and expensive to collect.

\subsection{Per-Connector vs. Global Models}

We use a dedicated ResNet18 model for each connector. Since every connector has unique geometry, cable angles, and lighting, a single "one-size-fits-all" model would fail on challenging cases like \texttt{conn6}. While this adds roughly 400\,MB to the storage footprint, it ensures the highest possible accuracy for each specific part.

\subsection{Thresholds as Operational Controls}

The "Max + 15\%" rule gives factory engineers a simple control dial. If the plant experiences new lighting noise, they can increase the safety margin (e.g., to 25\%) as a deployment setting without needing to retrain the AI. 

The continuous $P_{\mathrm{KO}}$ output also supports a \emph{grey zone} workflow: samples with $P_{\mathrm{KO}}$ near $\tau$ (borderline cases) can be flagged for mandatory human review, while samples far from $\tau$ in either direction receive automated pass/fail decisions. This stratified approach maximises throughput while maintaining safety margins.

\subsection{Comparison with Prior Approaches}

The PCL Presence pipeline achieves the best trade-off between accuracy, inference speed, memory footprint and operational simplicity. PatchCore's high recall came at prohibitive computational cost and without KO/OCCLUSION disambiguation. EfficientAD showed significant variability across different connector types. The synthetic negative approach eliminates the need for real KO training data entirely, which is the most practical advantage in a production environment with scarce defect samples.

\subsection{Limitations and Future Improvements}

The current pipeline assumes that the masked region is visually informative. When visibility is degraded due to obstruction, shadows or blur, the classifier produces elevated $P_{\mathrm{KO}}$ even for OK samples, since the visual evidence supporting ``cable present'' is weakened. This reflects genuinely missing information in the image rather than a conceptual failure of the model. The solution is upstream: improved camera placement or additional views to reduce obstruction.

Additionally, the synthetic KO generation assumes that inpainting produces a realistic approximation of an empty connector. For connectors where the cable significantly alters the surrounding texture (e.g.\ through shadow or colour bleed), the synthetic/real domain gap may increase, reducing classifier performance.

Practical upgrades for the next development cycle with bigger dataset available include: improved camera placement or additional views to reduce obstruction on conn6 and conn7; closed-loop data collection where operator-verified cases are added to the OK training pool; periodic mask refinement driven by false positive heatmaps and borderline $P_{\mathrm{KO}}$ analysis; investigation of lightweight architectures (MobileNetV3, EfficientNet-B0) to reduce per-model memory footprint below 10\,MB.

\section{Conclusions}

The final solution provides a scalable cable-detection system by creating realistic "fake" defects from good production data. This converts the difficult "needle-in-a-haystack" defect problem into a simple, reliable classification task that can be trained with minimal manual effort.

By combining specialized ResNet18 models with a strict "Max + 15\%" safety threshold, the system delivers high accuracy and clear confidence scores for operators. The resulting software is lightweight ($\sim$400\,MB), easy to reproduce, and can be updated for new connectors without needing an AI expert to retrain the entire pipeline.

% =============================================================================
% APPENDICES
% =============================================================================
\newpage
\appendix
\section{Exploratory Experiments and Technical Evolution}

The final pipeline emerged after two full development phases, each led by a different team member, that progressively clarified the constraints of the problem and exposed the limitations of general-purpose anomaly detection for a three-class industrial task. This appendix documents both phases in detail, as the technical insights they produced directly shaped the design choices of the final system.

\subsection{Phase 1: PatchCore Baseline with ResNet18}

The first phase investigated whether a generic CNN feature extractor paired with an unsupervised anomaly detector could cope with the extreme scarcity of KO samples and the confounding presence of occlusions.

\textbf{Feature extraction.} Each $128 \times 128$ connector crop is converted to three channels, normalized using ImageNet statistics and passed through a pretrained ResNet18 with its final classification layer removed. This yields a compact 512-dimensional global embedding $\mathbf{x} \in \mathbb{R}^{512}$ encoding shape, texture and local structure. All embeddings are stored for downstream analysis.

\textbf{PCA visualization.} Because the 512-dimensional feature space cannot be inspected directly, Principal Component Analysis was applied to project CNN embeddings onto 2D while preserving maximum variance.

\begin{figure}[H]
    \centering
    \begin{subfigure}{0.48\textwidth}
        \safeimg[width=\textwidth]{figures/pca_scatter.png}
        \caption{PCA scatter plot of ResNet18 embeddings. OK samples (blue) form a broad manifold; KO samples (orange) shift slightly outside it; OCCLUSION (green) and PARTIAL OCCLUSION (red) overlap with the OK region.}
    \end{subfigure}
    \hfill
    \begin{subfigure}{0.48\textwidth}
        \safeimg[width=\textwidth]{figures/patchcore_heatmap.png}
        \caption{PatchCore Anomaly heatmap example}
    \end{subfigure}
    \caption{}
    \label{fig:pca_patchcore}
\end{figure}
The scatter plot revealed several key structural patterns. OK samples form one broad, continuous cluster, confirming that the CNN embeddings capture normal variability across connectors, lighting conditions and board instances. KO samples appear as small groups slightly outside the OK manifold; since they are rare and visually subtle, they shift away from but do not cleanly separate from the OK cluster. OCCLUSION and PARTIAL OCCLUSION samples lie inside the OK region because occlusions alter local appearance without changing the overall connector geometry. The first principal component (PCA1) correlated with brightness, contrast and cable clutter, while PCA2 captured connector cavity depth, vertical shading and perspective variation.

\textbf{PatchCore anomaly detection.} PatchCore~\cite{roth2022patchcore} models only the appearance of normal (OK) data and flags deviations from that learned normality. It extracts intermediate feature maps from ResNet18 (up to \texttt{layer2}), producing spatial feature tensors $\mathbf{F} \in \mathbb{R}^{128 \times H_f \times W_f}$. 

These features are flattened into patch descriptors. Each patch represents a small local region capturing geometry, cable edges, shadows and cavity texture. The memory bank stores only OK patches: (1) extract and normalize each patch; (2) apply coreset sampling to reduce millions of patches to approximately 50,000 representative samples. 

For a test image, each patch is compared to the nearest memory entry and the anomaly score takes the maximum deviation.
A single strongly deviating region is therefore sufficient to classify the entire connector as anomalous. Patch-level distances are rearranged into a spatial anomaly heatmap where bright regions indicate patches that deviate strongly from the OK memory bank.

\textbf{Results.} Evaluation on the full labelled dataset ($N = 453$ scored images) showed that all 27 KO cases were correctly detected as anomalous, with only 8 OK samples becoming false positives. However, 171 full occlusions and 247 partial occlusions were also flagged as anomalous because, from the perspective of patch-level deviation, an occluding cable and a missing cable both look ``different from normal.''

\textbf{Limitations motivating Phase 2.} Four core limitations drove the transition away from PatchCore. First, the memory bank consumes significant RAM and becomes impractical when scaling to all nine connectors. Second, inference is slow because each test image requires millions of patch-to-memory distance computations. Third, It stores features but does not learn a dataset-specific representation, making it sensitive to illumination changes and connector variation. Fourth, the noisy patch-level distance maps produce unstable localization under shadows, reflections or low-contrast conditions.

\subsection{Phase 2: EfficientAD Teacher-Student Framework}

The second phase explored EfficientAD~\cite{batzner2023efficientad}, a state-of-the-art teacher-student anomaly detection framework that addresses PatchCore's efficiency limitations while offering sub-5\,ms inference latency \cite{batzner2023efficientad}.

\textbf{Architecture.} EfficientAD employs two networks. The \emph{Teacher} is a frozen ResNet18 pretrained on ImageNet, serving as a fixed feature extractor. The \emph{Student} is a randomly initialised ResNet18 with identical architecture, trained to mimic the Teacher's feature maps on OK images only. 

At test time, the squared difference between Teacher and Student feature maps is averaged across channels to produce a low-resolution anomaly map. The maximum value of this map becomes the scalar anomaly score. An additional autoencoder branch targets ``logical'' anomalies where a part is visually correct but structurally wrong (e.g.\ a cable routed incorrectly). The published benchmarks report Image-Level AU-ROC of 96.0\% and Pixel-Level AU-PRO of 93.3\% on MVTec AD and LOCO datasets with approximately 4.5\,ms latency per image.

\textbf{Training strategy.} All nine connector types underwent standard training of 70 epochs with intermediate checkpoints saved every 10 epochs for retrospective evaluation. Two input modalities were compared: raw RGB (preserving full colour information) and grayscale with CLAHE enhancement (focusing the network on structural differences). Model selection per connector was automated by a two-criterion logic: (1) primary: minimize false positives on the OK test set; (2) secondary: maximize the margin between the worst OK score and the best KO score. Safety thresholds were computed at 95\% of the minimum defect score.

\textbf{Per-connector results.} The following table summarises the raw RGB modality performance:

%%% Legacy Phase 2 Results (EfficientAD) moved to developmental reference section %%%
\begin{table}[H]
\centering
\small
\begin{tabular}{lccccc}
\toprule
Connector & Modality & Recall & Specificity & FP Rate \\
\midrule
conn1 & RGB & 1.00 & 1.00 & 0.0\% \\
conn2 & RGB & 1.00 & 0.97 & 3.1\% \\
conn5 & RGB & 1.00 & 1.00 & 0.0\% \\
conn6 & RGB & 0.25 & 0.98 & 1.8\% \\
conn7 & RGB & 1.00 & 1.00 & 0.0\% \\
conn8 & RGB & 1.00 & 1.00 & 0.0\% \\
conn9 & RGB & 1.00 & 0.70 & 30.3\% \\
\bottomrule
\end{tabular}
\caption{Performance of initial EfficientAD baseline (Raw RGB). Note the failure modes in conn6 and conn9 which motivated the final Synthetic Negative approach.}
\label{tab:efficientad_results}
\end{table}
\begin{figure}[H]
    \centering
    \safeimg[width=0.7\textwidth]{figures/confusion_matrices.png}
    \caption{EfficientAD confusion matrices. Perfect diagonal separation on conn1, conn5, conn7 and conn8. Conn9 shows 20 false positives; conn6 shows 3 missed defects.}
    \label{fig:efficientad_conf}
\end{figure}

Connectors 1, 5, 7 and 8 achieved perfect recall with zero false positives. However, two failure modes emerged that proved difficult to resolve within the anomaly detection paradigm.

\textbf{Conn6 (low recall, 0.25).} Three out of four defective samples were misclassified as OK. Score distribution analysis revealed that the anomaly scores for these defects were extremely low, overlapping significantly with the normal distribution. This indicated that the Student network had overgeneralised and treated the defect pattern as normal noise. The connector's PCL region is small relative to the background, so the small defect signal gets lost in the larger non-informative area.

\textbf{Conn9 (low specificity, 0.70).} Twenty out of sixty-six OK samples were incorrectly flagged as anomalous. The OK score distribution exhibited a long tail that extended past the safety threshold. Visual inspection suggested that these outliers contained partial occlusions or lighting noise, which the anomaly detector could not distinguish from true structural defects.

\textbf{Multi-step system (Phase 2b).} Building on Phase 2's insights, a multi-step pipeline was developed (led by Giovanni Passuello) that added a dedicated Occlusion Classifier in front of EfficientAD. The OcclusionCNN is a lightweight three-block convolutional network that classifies each connector crop as OCCLUSION or VISIBLE. The architecture uses Conv2d $\to$ BatchNorm $\to$ ReLU $\to$ MaxPool blocks, expanding from 32 to 128 channels, followed by a fully connected head with 512 neurons and dropout of 0.5. 

Only VISIBLE crops proceed to EfficientAD for OK/KO classification. This two-stage design achieved 95.13\% overall accuracy on the full 2{,}853-image dataset, with per-class accuracy of 98.16\% for OK, 84.00\% for KO and 81.03\% for OCCLUSION. The dominant error mode was OCCLUSION samples predicted as KO (56 cases, 11.5\% of occlusions), confirming that even with explicit occlusion filtering, partial occlusions visually resemble structural anomalies.

\subsection{Key Insights Carried Forward}

Across both phases, several insights directly shaped the final PCL Presence pipeline:

\begin{enumerate}[nosep]
    \item \textbf{Anomaly detection alone cannot solve the three-class problem.} Both PatchCore and EfficientAD treat everything ``different from OK'' as anomalous, making it inherently difficult to distinguish missing cables from occluding cables.
    \item \textbf{Per-connector models are essential.} A shared model trained across all connectors is dominated by the easiest cases (conn3, conn8) and fails on the hard ones (conn6, conn7). Each connector requires its own decision boundary.
    \item \textbf{The scarcity of real KO data is the binding constraint.} With fewer than 5 defects per connector, no supervised method can learn a robust KO representation directly. The solution must generate training signal from OK data alone.
    \item \textbf{Occlusions must be handled explicitly.} Relying on the anomaly detector to ignore occlusions produces unacceptable false positive rates. Either a dedicated occlusion classifier or a problem formulation that is inherently robust to occlusions is required.
    \item \textbf{Continuous scores enable operational tuning.} Both PatchCore and EfficientAD output continuous anomaly scores, enabling threshold-based sensitivity adjustment without retraining. This property was deliberately preserved in the final pipeline.
\end{enumerate}

These findings motivated the shift from anomaly detection to synthetic negative classification described in Section~4, where the problem is reformulated as binary presence detection using inpainting-generated training data.

\section{Acknowledgments}

We sincerely thank \textbf{Prof.\ Fredy Ruiz} for his guidance on balancing technical rigour with deployability. We are deeply grateful to \textbf{Marco Cederle} and \textbf{Davide Cazzaniga} from Beko Europe for their domain expertise and for facilitating our factory visits. Special thanks to the production operators for their practical feedback on our interface.


\end{document}